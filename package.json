{
  "name": "receipt-parser",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "test": "npm run test -w backend",
    "build": "npm run build -w packages/shared && npm run build -w backend && npm run build -w frontend",
    "clean": "rimraf backend/dist frontend/dist packages/shared/dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5"
  }
}
